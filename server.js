const express = require('express');
const app = express();
const port = 8000
app.use(cors())      

app.listen(port, () => log('The server is up and running on port: ${port}'));


const personRoute = require

app.listen(port, () => console.log())